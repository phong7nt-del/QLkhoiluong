const fs = require('fs');
// read from localStorage/sessionStorage isn't available in node directly.
// Let's modify DataStore to console.log it or we can just see the DataStore implementation of fetch data.
